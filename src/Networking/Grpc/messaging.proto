syntax = "proto3";

option csharp_namespace = "Drift.Networking.GrpcP2P";

package messaging;

service Messenger {
  rpc SendMessage (MessageRequest) returns (MessageReply);
}

message MessageRequest {
  string from = 1;
  string text = 2;
}

message MessageReply {
  string status = 1;
}